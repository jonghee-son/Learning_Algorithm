#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    int a, b, c, d;
    

    scanf("%d %d", &a, &b);

    int* arr = malloc(a * 6);

    for (int i = 0; i < a; i++) {
        scanf("%s", &arr);
    }

    while (arr[d] != ' ') {
        
    }

    for (int i = 0; i < a; i++) {
        if (arr < b) {
            printf("%d ", c);
        }
    }

    return 0;
}